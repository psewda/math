language: go
go: [1.14]

stages:
  - build
  # - test
  - deploy

jobs:
  include:
    - stage: build
      script: make build
    # - stage: test
    #   script: make test
    - stage: deploy
      script: |
        make build
        pwd
        echo $TRAVIS_BUILD_DIR
        cd $TRAVIS_BUILD_DIR/bin
        tar -zcvf pie-$(cat version).$TRAVIS_BUILD_NUMBER ./*
        cd $TRAVIS_BUILD_DIR
